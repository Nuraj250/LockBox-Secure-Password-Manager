# Configuration file for the password manager

DB_PATH = "database/lockbox.db"  # Database file path
KEY_FILE = "database/key.key"    # Encryption key file path

# UI settings (if needed in future updates)
APP_NAME = "LockBox Secure Password Manager"
APP_VERSION = "1.0"
